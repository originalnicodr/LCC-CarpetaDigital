;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |6)|) (read-case-sensitive #t) (teachpacks ((lib "image.rkt" "teachpack" "2htdp") (lib "universe.rkt" "teachpack" "2htdp"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "image.rkt" "teachpack" "2htdp") (lib "universe.rkt" "teachpack" "2htdp")))))
(define (string-remove-last s) (if (not(string=? "" s)) (substring s 0 (- (string-length s) 1)) s))
(define (escribir n) (place-image/align (text n 20 "indigo")
                   0 0 "left" "top" (empty-scene 800 60)))
(define (teclado l c) (if (string=? c "\b") (string-remove-last l) (string-append l c)))
(big-bang "" [to-draw escribir]
             [on-key teclado])