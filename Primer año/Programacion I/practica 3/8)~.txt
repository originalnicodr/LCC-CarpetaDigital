;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |8)|) (read-case-sensitive #t) (teachpacks ((lib "image.rkt" "teachpack" "2htdp") (lib "universe.rkt" "teachpack" "2htdp"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "image.rkt" "teachpack" "2htdp") (lib "universe.rkt" "teachpack" "2htdp")))))
(define ANCHO 600)
(define ALTO 800)
(define scolor "white")
(define ssize 40)

(define cielo (place-image (rectangle ANCHO ALTO "solid" "blue") (/ ANCHO 2) (/ ALTO 2) (empty-scene ANCHO ALTO))) ;cielo azul
(define (teclado a k) (if (string=? k "\b") cielo a))
(define (click a x y e) (overlay/xy (star ssize "solid" scolor) (- 0 x) (- 0 y) a)) ;agrega estrellas
(define (dcielo a 

(big-bang cielo [to-draw cielo]
                [on-key teclado]
                [on-mouse click])

(place-image (star ssize "solid" scolor) 0 0 (empty-scene ANCHO ALTO))